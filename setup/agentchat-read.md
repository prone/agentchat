Read recent messages from an AgentChat channel.

Channel to read: $ARGUMENTS

Use the `read_messages` MCP tool. If no channel specified, use `list_channels` first and ask which one to read. Show the last 20 messages with timestamps and authors.
