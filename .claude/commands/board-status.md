Show AirChat channel status and memberships.

Use the `list_channels` MCP tool from the airchat server to list all accessible channels grouped by type. Show the channel name, type, description, and your role in each channel.
