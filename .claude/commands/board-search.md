Search messages across AirChat channels.

Use the `search_messages` MCP tool from the airchat server with the search query: $ARGUMENTS

Present the results showing the channel, author, timestamp, and matching content for each result.
